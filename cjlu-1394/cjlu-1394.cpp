#pragma warning(disable:4996)
#include <iostream>
#include <math.h>
#include <vector>
#include <algorithm>
#include <string.h>
using namespace std;
int main()
{
	std::ios::sync_with_stdio(false);
	std::cin.tie(0);
	int n;
	while (cin >> n)
	{
		int x;
		cin >> x;
		int i = 0, t;
		int* b = new int[x];
		for (; i < x; i++)
		{
			cin >> b[i];
		}
		for (i = 0; i < x; i++)
		{
			
		}
		for (i = 1; i <= 10000; i++)
		{
			f(i,i&1);
		}
		if (a[n])cout << "Victor" << endl;
		else cout << "Jenny" << endl;
	}

	return 0;
}

